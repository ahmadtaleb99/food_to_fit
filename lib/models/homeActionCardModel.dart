import 'package:flutter/material.dart';

class HomeActionCardObject {
  Color? backgroundColor;
  Color? iconBackgroundColor;
  IconData? icon;
  String? title;

  HomeActionCardObject(
      {this.backgroundColor, this.iconBackgroundColor, this.icon, this.title});
}